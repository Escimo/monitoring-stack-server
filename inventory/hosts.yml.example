---
all:
  hosts:
    monitoring:
      ansible_host: monitoring.example.com  # Or IP Address 
      ansible_user: ubuntu  # SSH User
      ansible_ssh_private_key_file: ~/.ssh/your-key.pem  # Path to SSH-Key
      ansible_become: yes  # root